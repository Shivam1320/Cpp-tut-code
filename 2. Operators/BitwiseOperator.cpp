// Bitwise Operator works on Bits i.e, o and 1
// AND (&), OR (||), NOT (~), XOR (^)
// ~ --> tilda



#include<iostream>
using namespace std;
int main(){

    // bool a = false;
    // bool b = false;

    //cout<< (a & b);

    //cout<< (a^b);

    //int a = 5;
    bool a = false;
    cout<< ~a <<endl;
    cout<< ~(a) <<endl;
    cout<< (~a) <<endl;




  return 0;
}